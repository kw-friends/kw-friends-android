<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KWFriends"
        tools:targetApi="31">
        <!--TODO activity 에러 해결-->
        <activity
            android:name=".AuthViewModel"
            android:exported="false"
            android:label="@string/title_activity_auth_view_model"
            android:theme="@style/Theme.KWFriends" />
        <activity
            android:name=".AuthUiState"
            android:exported="false"
            android:label="@string/title_activity_auth_ui_state"
            android:theme="@style/Theme.KWFriends" />
        <activity
            android:name=".AuthScreen"
            android:exported="false"
            android:label="@string/title_activity_auth_screen"
            android:theme="@style/Theme.KWFriends" />
        <activity
            android:name=".auth.AuthMainActivity"
            android:exported="true"
            android:label="@string/title_activity_auth_main"
            android:theme="@style/Theme.KWFriends">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.lib_name"
                android:value="" />
        </activity>
    </application>

</manifest>